---
ansible_connection: network_cli
ansible_network_os: community.routeros.routeros
ansible_user: admin
# ansible_ssh_pass: set in vault or passed via --ask-pass
# ansible_ssh_private_key_file: /path/to/key  # preferred over password
